const chalk = require('chalk')

// log helper constants
const error = chalk.red
const warning = chalk.yellow
const success = chalk.green
const highlight = chalk.bold.cyan

module.exports = {
    error,
    warning,
    success,
    highlight
}